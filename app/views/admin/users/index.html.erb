<table class="table">
	<thead class="text-center">
		<tr>
			<th>id</th>
			<th>ユーザー名</th>
			<th>登録日</th>
			<th>タスク数</th>
			<th>権限</th>
		</tr>
	</thead>
	<tbody>
		<tr>
		<% @users.each do |u| %>
			<td><%= u.id %></td>
			<td><%= u.name %></td>
			<td><%= u.created_at %></td>
			<td><%= u.tasks.count %></td>
			<td><%= u.admin %></td>
			<td><%= link_to t('.edit'), edit_admin_user_path(u) %></td>
			<td><%= link_to t('.show'), admin_user_path(u) %></td>
			<td><%= link_to t('.destroy'), u, method: :delete, data: { confrim: '削除しますか？'} %></td>
		<% end %>
		</tr>
	</tbody>
</table>
